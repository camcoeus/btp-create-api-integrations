{
  "name": "cron-job",
  "version": "1.0.0",
  "description": "Cron Job",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "cross-var": "^1.1.0"
  },
  "scripts": {
    "deploy:kyma": "cross-var helm upgrade --install cron-job ./chart --namespace $npm_config_namespace",
    "undeploy:kyma": "cross-var helm uninstall cron-job ./chart --namespace $npm_config_namespace"
  }
}
